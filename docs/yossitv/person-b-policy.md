
これからフロントエンドを実装します。
ダークテーマは作らず、共通化させるものとします。



`root: /` 
ヘッダーファイル gitのレポジトリー 
body はプロジェクトのpitchを入れる場所とします。

`root: /dashboard`
実際の見せるための実装

詳しい要件

/dashboard
最新の状況をフロントエンドで取得できる状態のフロントエンドを作成します。
バックエンドサーバーのエンドポイントを取得し、設計をします。

uiとしては
左上にプロジェクト名というか、状態を現状の実装状況を把握し、できる限り実装ができる状態を作成してください

backendのapiを取得し、設計すること
デザインはハッカー風の紺色、白のパーティション系のかっこいいUIとします。
https://github.com/ayushozha/deepops表示できるようにします。
インシデントのログを左へ表示できる状態（レベル含めて表示されるものとします）
右へaiが書いた状態でのdiffでずれを全てのプロジェクトに跨ったファイルの差分を取得し表示できる状態
右下に approve, rejectするボタンを配置します。


色のデザインとして、hacker風のデザインとする。
