version: 1
scope: person-b
owner: yossitv
requirements:
- frontend-requirements.md
- person-b-policy.md
- oauth-requirements.md
execution_notes:
- PB-001 と PB-002 と PB-003 は並列で着手する。
- PB-004 と PB-005 と PB-006 は PB-001 の型と hooks 契約を前提に進めるが、prop 駆動で先に UI を作ってよい。
- PB-007 は dashboard が通しで動く状態を作る締めの統合タスクにする。
- PB-008 で Auth0 SDK v4 の導入方針と `/auth/*` ルートを固定してから PB-009 と PB-010 に着手する。
- PB-009 は公開 landing と auth 導線、PB-010 は dashboard の session-aware UI に集中し、最後に PB-011
  で callback / env / build を通しで確認する。
context:
  current_state:
  - frontend の `/` と `/dashboard` は実装済みで、固定ダークテーマ、GitHub 導線、ハッカー風の 3 カラム dashboard
    shell が入っている。
  - frontend/app/dashboard/types.ts, hooks/useIncidents.ts, hooks/useApproval.ts,
    hooks/useBackendHealth.ts を追加し、backend 契約、SSE、定期再取得、approval 反映、health polling
    を閉じた。
  - frontend/app/dashboard/components/IncidentList.tsx, DiffViewer.tsx, ApprovalButtons.tsx,
    SeverityBadge.tsx, StatusBadge.tsx を実装し、dashboard/page.tsx で実データ接続まで統合した。
  - frontend/next.config.ts に `/api/:path*` の proxy rewrites を追加し、`NEXT_PUBLIC_API_BASE_URL`
    経由で FastAPI backend に接続できる。
  - backend 側は /api/health, /api/incidents, /api/incidents/{id}, /api/incidents/stream,
    /api/approval/{incident_id}/decision を実装済みで、SSE の custom event 名も要件と整合している。
  - GET /api/incidents は bare array を返し、POST /api/approval/{id}/decision は incident
    を含むラップ済みオブジェクトを返す。
  - 2026-03-28 時点で実装後の `cd frontend && npm run lint` と `cd frontend && npm run build`
    は成功している。
  - ただし frontend には Auth0 Next.js SDK v4, `frontend/lib/auth0.ts`, `frontend/proxy.ts`,
    login/logout/profile UI, session-aware な `/dashboard` の認証分岐はまだ入っていない。
  - OAuth 要件は `docs/yossitv/oauth-requirements.md` と `docs/hackathon-context/auth0-nextjs-quickstart.md`
    の `/auth/*` 前提を採用し、古い `/api/auth/*` ルートは使わない。
  agent_guidance:
    codex.task: 推論を長く使い、契約整理、hooks / state 同期、proxy、統合、検証を担当する。
    claude.task: UI 実装、コンポーネント分割、見た目の作り込みを高速に進める。
completion_log:
- date: '2026-03-28'
  actor: codex.task
  summary:
  - PB-001 を完了し、dashboard の型、incident 同期 hook、approval hook、health hook、Next.js proxy
    rewrites を実装した。
  - subagent を使って PB-002 から PB-006 の UI 実装を進め、landing page と dashboard components
    を作成した。
  - PB-007 を完了し、dashboard を backend API と接続、approval 後の再取得と health/SSE 監視を統合し、lint/build
    を通した。
generated_from: task.yml
agent: claude.task
tasks:
- task_id: PB-002
  title: "`/` のランディングページを DeepOps の pitch ページへ置き換える"
  description: |
    `frontend/app/page.tsx` を create-next-app の初期画面から置き換える。
    DeepOps のプロジェクト名、GitHub リポジトリリンク、キャッチコピー、アーキテクチャ概要、スポンサーツール一覧、`/dashboard` への CTA を配置する。
    `frontend/app/layout.tsx` の metadata も DeepOps 向けに更新する。
    このタスクは backend 依存がないので単独で先行実装してよい。
    完了条件:
    - `/` が pitch ページとして成立している
    - `/dashboard` への導線が視認できる
  status: completed
  agent: claude.task
- task_id: PB-003
  title: "`/dashboard` の固定ダークテーマと 3 カラム shell を作る"
  description: |
    `frontend/app/globals.css` を固定ダークテーマに置き換え、`prefers-color-scheme` 依存をなくす。
    `frontend/app/dashboard/page.tsx` を作成し、ヘッダー、左カラム、右上 diff 領域、右下 approval 領域を持つ 3 カラム shell を作る。
    `frontend/app/dashboard/components/SeverityBadge.tsx` と `frontend/app/dashboard/components/StatusBadge.tsx` を作り、要件の色定義を反映する。
    ヘッダー中央の接続状況バッジは prop で受けられるようにし、PB-007 で health polling を接続しやすい形にしておく。
    完了条件:
    - `/dashboard` のレイアウトが要件どおりのパネル構成になっている
    - 色、罫線、フォントがハッカー風テーマに揃っている
  status: completed
  agent: claude.task
- task_id: PB-004
  title: 左パネルの IncidentLog を prop 駆動コンポーネントとして実装する
  description: |
    `frontend/app/dashboard/components/IncidentList.tsx` を作り、incident 一覧、selected id、onSelect を受け取る純粋な UI コンポーネントとして実装する。
    表示項目は severity、短縮 incident id、status、service + environment、error_type + error_message、相対時刻。
    一覧はスクロール可能にし、選択中の incident が明確にわかる視覚表現にする。
    PB-001 の hook 接続前でもモック props で確認できる構造にする。
    完了条件:
    - Incident 一覧の見た目と情報密度が要件を満たす
    - page 層からデータと選択状態を渡すだけで使える
  status: completed
  agent: claude.task
- task_id: PB-005
  title: 右上の DiffViewer と incident 詳細パネルを実装する
  description: |
    `frontend/app/dashboard/components/DiffViewer.tsx` を作り、選択中 incident を受けて以下を表示する:
    - `fix.files_changed`
    - unified diff 形式の `fix.diff_preview`
    - `fix.spec_markdown` のアコーディオン
    - `diagnosis.root_cause`
    - `diagnosis.confidence`
    - 任意で `diagnosis.suggested_fix`, `fix.test_plan`, `approval.channel`
    diff は追加、削除、文脈行で色分けし、incident 未選択時はプレースホルダーを出す。
    完了条件:
    - incident 選択時に detail パネルとして十分な情報が見える
    - incident 未選択時の空状態が崩れない
  status: completed
  agent: claude.task
- task_id: PB-006
  title: 右下の ApprovalButtons を実装し、承認 UI の条件分岐を固定する
  description: |
    `frontend/app/dashboard/components/ApprovalButtons.tsx` を作り、選択中 incident と approve / reject handler を受ける UI コンポーネントとして実装する。
    表示条件は `incident.status === "awaiting_approval"` かつ `incident.approval.status === "pending"` に限定する。
    送信中の disabled 制御、二重送信防止、成功時に `response.incident` を上位 state に戻しやすい props 設計にする。
    PB-001 の `useApproval` に素直に接続できる shape を優先し、UI 側で approval payload を組み立て直さない。
    完了条件:
    - approval が不要な incident でボタンが出ない
    - pending incident だけ approve / reject が押せる
  status: completed
  agent: claude.task
- task_id: PB-008
  title: Auth0 Next.js SDK v4 の基盤を frontend に導入し `/auth/*` ルートを有効化する
  description: |
    `docs/yossitv/oauth-requirements.md` と `docs/hackathon-context/auth0-nextjs-quickstart.md` を基準に、frontend に Auth0 v4 の土台を入れる。
    `frontend/package.json` に `@auth0/nextjs-auth0` を追加し、`frontend/lib/auth0.ts` で `new Auth0Client()` を export する。
    さらに `frontend/proxy.ts` を追加して quickstart どおりの `/auth/*` ルートを有効化し、dynamic route handler や古い `/api/auth/*` 実装は作らない。
    `.env.local` / repo root `.env` で必要な環境変数名と `AUTH0_REDIRECT_URI=http://localhost:3000/auth/callback` の前提が崩れないよう、参照箇所や README を必要最小限更新してよい。
    完了条件:
    - frontend で Auth0 SDK v4 が参照可能
    - `/auth/login` と `/auth/logout` の導線を置ける基盤がある
    - `proxy.ts` 前提の構成で `/auth/*` を使う方針がコード上でも明確
  status: pending
  agent: claude.task
- task_id: PB-009
  title: 公開 landing と共通 auth UI に login/logout/profile 導線を追加する
  description: |
    `frontend/components/auth/LoginButton.tsx`, `LogoutButton.tsx`, `Profile.tsx` を追加し、anchor ベースの `/auth/login` / `/auth/logout` 導線を実装する。
    `frontend/app/page.tsx` は公開ページのまま維持しつつ、未認証時に login CTA を見せ、認証済みなら dashboard への導線とユーザー表示名または email を見せられるようにする。
    既存の DeepOps の見た目は保ちつつ、Auth0 quickstart の最小 UI 要件を満たすことを優先する。
    完了条件:
    - `/` から login を開始できる
    - logout 導線と profile 表示が実装されている
    - auth UI が既存 landing のデザインを壊さず統合されている
  status: pending
  agent: claude.task
- task_id: PB-010
  title: "`/dashboard` を session-aware にし未認証時の login prompt と認証済み承認 UI を分岐する"
  description: |
    `auth0.getSession()` を使って `/dashboard` の表示を認証状態で分岐させる。
    未認証時は dashboard 全体を壊さず login CTA または read-only 表示にし、`APPROVE` / `REJECT` ボタンは出さない。
    認証済み時は既存の incident list, diff viewer, health 状態を維持したまま approval UI を表示し、ヘッダーまたは適切な位置で user identity を確認できるようにする。
    server component と client component の責務分割は実装しやすい形で見直してよいが、UI 側で session 有無の条件分岐が明確であることを優先する。
    完了条件:
    - `/dashboard` が session の有無で login prompt か本来の dashboard を出し分ける
    - 未認証ユーザーに `APPROVE` / `REJECT` が見えない
    - 認証済みユーザー名または email が UI で確認できる
  status: pending
  agent: claude.task
- task_id: PB-011
  title: OAuth callback と環境変数前提を確認し lint/build まで通す
  description: |
    Auth0 の callback / logout / origin 前提を `http://localhost:3000` と `/auth/callback` に合わせ、古い `/api/auth/callback` 前提が残っていないか確認する。
    必要なら `frontend/README.md` か関連ドキュメントに、ローカルで必要な Auth0 設定値と手動確認手順を短く追記する。
    最後に `cd frontend && npm run lint` と `cd frontend && npm run build` を実行し、OAuth 導入後も Person B frontend が壊れていないことを確認する。
    完了条件:
    - `AUTH0_REDIRECT_URI` と callback URL の前提が `/auth/callback` に揃っている
    - 手動確認に必要な最小限の情報が repo 内に残っている
    - frontend の lint/build が通る
  status: pending
  agent: claude.task
